%h2.no-top= @page_title = "Toutes les startups"
= link_to 'Ajouter une nouvelle startup', new_admin_startup_path, class: "btn btn-primary"
%table.table.table-condensed.table-hover.table-striped
  %tbody
    - for startup in @startups
      %tr
        %td= image_tag(startup.logo.url(:small)) if startup.logo.file?
        %td= startup.name
        %td= I18n.l startup.added_on, format: :added_on if startup.added_on
        %td
          %ul.list-inline
            %li= link_to fa_icon('edit', title: 'Modifier'), [:edit, :admin, startup]
            %li= link_to fa_icon('trash', title: 'Supprimer'), [:admin, startup], method: :delete, data: {confirm: 'Voulez-vous vraiment supprimer cette startup ?'}

= will_paginate @startups, renderer: BootstrapPagination::Rails
