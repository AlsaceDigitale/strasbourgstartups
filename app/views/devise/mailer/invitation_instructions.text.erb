<%- startup_name = @resource.startups.first.name %>
<%= t("devise.mailer.invitation_instructions.hello", first_name: @resource.first_name) %>

<%= t("devise.mailer.invitation_instructions.someone_invited_you", url: root_url, startup_name: startup_name) %>

<%= accept_user_invitation_url(@resource, invitation_token: @token) %>

<% if @resource.invitation_due_at %>
  <%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %>
<% end %>

Yann Klis, pour le groupe Strasbourg Startups

<%= strip_tags t("devise.mailer.invitation_instructions.ignore", startup_name: startup_name) %>
